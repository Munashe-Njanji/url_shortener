# Core Framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2

# Database
SQLAlchemy==2.0.35
psycopg[binary]==3.2.3  # PostgreSQL driver (newer version, better for Python 3.13)
alembic==1.13.1  # Database migrations

# Caching and Task Queue
redis==5.0.8
celery==5.4.0

# Security
python-jose==3.4.0  # JWT tokens
passlib[bcrypt]==1.7.4  # Password hashing
python-multipart==0.0.9  # Form data parsing

# HTTP Client
httpx==0.27.0  # For async HTTP requests
requests==2.32.3  # For sync HTTP requests

# Utilities
python-dateutil==2.9.0.post0
python-dotenv==1.0.1  # Environment variable loading
psycopg2
email-validator

# Monitoring and Logging
prometheus-client==0.20.0  # Metrics
sentry-sdk[fastapi]==2.14.0  # Error tracking

# Development
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
black==24.8.0  # Code formatting
flake8==7.1.1  # Linting
mypy==1.11.2  # Type checking

# Optional: For production features
# stripe==10.8.0  # Payment processing
# boto3==1.35.0  # AWS S3 for QR codes
# clickhouse-driver==0.2.9  # Analytics database
# qrcode[pil]==7.4.2  # QR code generation
anyio>=4.4.0 # not directly required, pinned by Snyk to avoid a vulnerability
starlette>=0.49.1 # not directly required, pinned by Snyk to avoid a vulnerability
urllib3>=2.5.0 # not directly required, pinned by Snyk to avoid a vulnerability
zipp>=3.19.1 # not directly required, pinned by Snyk to avoid a vulnerability
dnspython>=2.6.1 # not directly required, pinned by Snyk to avoid a vulnerability
pyasn1>=0.6.2 # not directly required, pinned by Snyk to avoid a vulnerability
